# terminal-setup
The simplest Tmux + Neovim ever (Wezterm for Windows users).
I’m trying to keep the defaults as much as possible.
## note: This configuration is for Neovim 0.12+.
## setup
To install, run:
```bash
./setup.sh install
```
To remove, run:
```bash
./setup.sh remove
```
